import { pool } from "../db";
import express from "express";

const router = express.Router();


// authentication
// expecting user id and CHATROOM id because auth is used to get chatroom messages.

